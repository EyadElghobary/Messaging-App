package com.example.demo.types.chat;

public enum MessageType {
    
    // This for when the user sends a chat
    CHAT,

    // This is when a user joins the chat
    JOIN,

    // This is when a user leaves the chat.
    LEAVE
}
